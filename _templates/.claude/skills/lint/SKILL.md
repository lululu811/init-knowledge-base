---
name: lint
description: 知识库全局健康度检查。扫描 wiki/ 目录，检测死链、孤儿页面、未同步索引、重复内容、知识冲突、来源缺失和页面陈旧度。当用户输入 /lint、/scan、/health 或要求"检查知识库状态"时调用。
user-invocable: true
---

# lint 技能：知识图谱健康巡检

## 核心目标
定期运行此 skill，找出知识库长期演进中产生的结构性问题和质量问题。

## 触发条件
- 用户输入 `/lint`
- 用户询问"我的知识库健康状况如何"
- 用户要求"检查知识库状态"或"检查健康"

## 巡检流水线

### 第 1 步：索引一致性检查
1. 读取 `wiki/index.md` 全部内容
2. 扫描 `wiki/` 下所有 `.md` 文件（排除 index.md 和 log.md）
3. 提取 index.md 中注册的所有双链链接 `[[页面名称]]`
4. 比对：找出已注册但文件不存在的条目 OR 文件存在但未注册的页面

### 第 2 步：双向链接健康检查
1. 扫描所有 `.md` 文件，提取所有 `[[双链]]` 格式的链接
2. 如果链接指向的页面不存在 → 标记为**死链**
3. 统计被引用的页面
4. 找出从未被任何其他页面引用的页面 → **孤儿页面**

### 第 3 步：认知冲突审查
1. 全局搜索所有 `.md` 文件中包含 `## 知识冲突` 的页面
2. 提取每个冲突的简要描述
3. 统计带未解决冲突的页面

### 第 4 步：来源可追溯性检查
1. 扫描 `wiki/` 下所有带有 frontmatter 的页面
2. 检查 `sources` 字段：
   - 页面类型为 `source` → 必须有 `sources` 指向 raw/ 文件
   - 页面类型为 `concept`/`entity` → 建议有 `sources`
   - 页面类型为 `synthesis` → 必须有多个 `sources`
3. 标记 **来源缺失页面**

### 第 5 步：页面陈旧度检查
1. 读取每个页面的 `last_updated` 和 `created` 字段
2. 计算距今天数：
   - 超过 90 天未更新 → 标记为**陈旧页面**
   - 超过 180 天未更新 → 标记为**高度陈旧**
3. 特别关注 `status = "draft"` 且超过 30 天的页面 → **长期草稿**

### 第 6 步：重复内容检测
1. 提取所有 wiki 页面的纯文本内容（排除 frontmatter）
2. 计算页面间的文本相似度：
   - 使用简单启发式：提取前 200 个字符 + 关键词集合的 Jaccard 相似度
   - 相似度 > 0.7 → 标记为**潜在重复**
   - 相似度 > 0.85 → 标记为**高度疑似重复**
3. 报告重复页面组

> 注意：重复检测是计算密集型操作，首次运行可能较慢。结果仅供参考，需人工判断。

### 第 7 步：知识质量评分
对每个页面进行快速质量评估：

| 维度 | 标准 | 权重 |
|------|------|------|
| 有定义/摘要 | 页面有"一句话定义"或"核心摘要"区块 | 20% |
| 有双向链接 | 页面至少包含 2 个出站链接 | 20% |
| 有来源 | 页面 `sources` 字段非空 | 20% |
| 有更新 | 90 天内更新过 | 20% |
| 有结构化内容 | 页面包含 3 个以上标题区块 | 20% |

计算每页得分，标记**低质量页面**（得分 < 40%）。

## 报告输出规范

```markdown
## 🩺 知识库健康体检报告 — YYYY-MM-DD

### 📊 概览统计
| 指标 | 数值 |
|------|------|
| 总页面数 | N |
| 平均质量分 | N% |
| 健康度评级 | 🟢 良好 / 🟡 一般 / 🔴 需关注 |

### ✅ 绿灯项
- [运行良好的项目]

### ⚠️ 黄灯项
- **N 个孤儿页面**: [列表]
- **N 个未同步索引**: [列表]
- **N 个长期草稿**: [列表]
- **N 个陈旧页面**: [列表]
- **N 个来源缺失**: [列表]

### ❌ 红灯项
- **N 个死链**: [来源页面] → [[不存在的目标页面]]
- **N 个未解决的知识冲突**: [页面名称]
- **N 组潜在重复**: [页面 A] ≈ [页面 B] (相似度 X%)
- **N 个低质量页面**: [列表]

### 🛠️ 下一步行动
1. 是否需要自动修复未同步索引？
2. 是否需要针对知识冲突进行重新推演？
3. 是否需要合并/删除重复页面？
```

## 硬约束
- **仅读扫描**：生成报告前，禁止修改任何文件
- **手动确认**：报告后等待用户确认再执行修复
- **记录日志**：修复完成后，在 wiki/log.md 追加 lint 记录

## 修复模式（用户确认后）

用户确认后，可执行以下自动修复：

1. **同步索引**：将未注册的页面自动添加到 index.md 对应分类
2. **标记死链**：在死链所在页面添加 `<!-- DEAD_LINK: [[页面]] -->` 注释
3. **更新陈旧标记**：在高度陈旧页面顶部添加 `> ⚠️ 本页面超过 180 天未更新，内容可能过时。` 的 callout
