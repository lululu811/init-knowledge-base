---
name: refresh
description: 联网更新知识库中的过期内容。扫描 wiki/ 页面，识别陈旧或可能过时的知识，通过联网搜索获取最新信息，对比更新或标记冲突。支持 /refresh（全局扫描）、/refresh <页面名>（指定页面）、/refresh --tag=<标签>（按标签批量）。当用户提到"更新知识库"、"刷新"、"检查时效性"、"联网更新"、"知识过期"时触发。
user-invocable: true
---

# refresh 技能：联网时效性更新

## 核心目标

知识库会随时间推移而过时。本技能通过联网搜索，主动验证和更新 wiki/ 中的陈旧内容，确保知识的时效性和准确性。

## 触发场景
- 用户输入 `/refresh` — 全局扫描陈旧页面并联网更新
- 用户输入 `/refresh <页面名>` — 更新指定页面
- 用户输入 `/refresh --tag=<标签>` — 按标签批量更新
- 用户说"知识库该更新了"、"检查一下过时内容"、"联网刷新知识库"

## 状态追踪机制

通过 frontmatter 中的 `last_refreshed` 字段和 `.claude/refresh-state.json` 追踪刷新状态：

```json
{
  "version": 1,
  "last_full_scan": "2026-08-15T10:00:00",
  "pages": {
    "wiki/concepts/Transformer.md": {
      "last_refreshed": "2026-08-15T10:30:00",
      "status": "current",
      "changes": ["更新了参数规模数据", "补充了 GPT-5 相关引用"],
      "sources_added": ["https://example.com/new-info"]
    }
  }
}
```

### 状态规则

| 状态 | 含义 |
|------|------|
| `current` | 联网验证过，内容仍然准确 |
| `updated` | 联网后发现新信息，已更新页面 |
| `conflict` | 联网后发现矛盾信息，已标记冲突 |
| `stale` | 超过刷新周期，待处理 |
| `failed` | 上次刷新失败（网络错误等），需重试 |

---

## 刷新流水线

### 步骤 1：识别刷新候选

**全局扫描模式（`/refresh`）：**
1. 读取 `wiki/` 下所有带 frontmatter 的 `.md` 文件（排除 index.md、log.md）
2. 检查 `last_refreshed` 字段（若无此字段，使用 `last_updated`）
3. 按优先级排序候选页面：
   - **高优先**：超过 90 天未刷新 + 主题属于快变领域（AI/技术/商业/政策）
   - **中优先**：超过 90 天未刷新 + 主题属于慢变领域（科学/历史/哲学）
   - **低优先**：超过 180 天未刷新 + 主题属于稳定领域
   - **跳过**：30 天内已刷新且状态为 `current`
4. 状态为 `draft` 的页面也纳入候选（可能有信息缺口可补充）

**指定页面模式（`/refresh <页面名>`）：**
直接定位目标页面，跳过扫描步骤。

**按标签模式（`/refresh --tag=<标签>`）：**
扫描带有指定标签的所有页面。

### 步骤 2：提取搜索关键词

对每个候选页面：
1. 读取完整内容
2. 提取页面标题作为核心搜索词
3. 从 frontmatter 的 `tags` 提取辅助关键词
4. 从"一句话定义"或"核心摘要"提取关键概念
5. 构造搜索查询：
   - 基础查询：`{标题} {关键概念} 最新 2025 2026`
   - 补充查询：`{标题} 更新 变化 新进展`
   - 实体查询：`{实体名} 最新动态 新闻`

### 步骤 3：联网搜索

1. 使用搜索工具执行步骤 2 构造的查询
2. 读取排名前 3-5 的搜索结果
3. 提取关键事实：
   - 新数据/新数字（市值、参数量、版本号等）
   - 新事件（融资、发布、人事变动等）
   - 新观点/新论文
   - 对已有信息的验证或推翻

### 步骤 4：对比分析

将搜索结果与现有 wiki 内容逐项对比：

| 情况 | 处理方式 |
|------|---------|
| 新信息补充现有内容 | 标记为"待追加" |
| 新信息与现有内容矛盾 | 标记为"待冲突处理" |
| 现有内容被验证仍准确 | 标记为"已验证" |
| 搜索结果与主题无关或无新信息 | 标记为"无更新" |
| 发现新实体/概念需创建页面 | 标记为"待创建" |

### 步骤 5：更新页面

**更新类型 A — 追加新信息：**

在页面的相应区块追加新内容，并在末尾添加时效性更新标记：

```markdown
## 时效性更新

> [!info] 联网刷新 — YYYY-MM-DD
> 已通过联网搜索验证并更新本页面内容。

- **追加**：[新信息摘要] — 来源：[URL]
- **更新**：[旧数据] → [新数据] — 来源：[URL]
```

**更新类型 B — 标记冲突：**

在页面的 `## 知识冲突` 区块追加：

```markdown
### [YYYY-MM-DD] 联网发现的冲突

| 知识库现有说法 | 联网最新信息 | 来源 |
|---------------|-------------|------|
| [旧内容] | [新内容] | [URL] |

> [!warning] 需要人工确认
> 联网发现的信息与知识库现有内容存在冲突，请核实后决定保留哪个版本。
```

**更新类型 C — 创建新页面：**

如果搜索中发现重要的新实体或概念，按照 ingest 流程创建新页面。

### 步骤 6：更新 frontmatter

更新页面的 YAML 头部：
```yaml
last_updated: "YYYY-MM-DD"
last_refreshed: "YYYY-MM-DD"
```

如果搜索结果中有值得留存的来源，在 `sources` 字段追加 URL。

### 步骤 7：更新全局注册表

**更新 `wiki/index.md`：**
如果创建了新页面，添加到对应分类。

**更新 `wiki/log.md`：**
```markdown
## [YYYY-MM-DD] refresh | <操作简述>
- **扫描范围**: N 个页面
- **已更新**: [[PageA]], [[PageB]]
- **标记冲突**: [[PageC]]
- **已验证**: [[PageD]], [[PageE]]
- **新增来源**: N 条 URL
```

### 步骤 8：更新状态记录

在 `.claude/refresh-state.json` 中更新每个已处理页面的记录。

---

## 报告输出规范

```markdown
## 联网刷新报告 — YYYY-MM-DD

### 概览
| 指标 | 数值 |
|------|------|
| 扫描页面数 | N |
| 联网搜索次数 | N |
| 内容已更新 | N 页 |
| 冲突已标记 | N 页 |
| 已验证仍准确 | N 页 |
| 无相关信息 | N 页 |
| 新增来源链接 | N 条 |

### 已更新页面
- [[PageA]] — 更新了参数规模数据；补充了 2026 年新论文引用
- [[PageB]] — 更新了公司市值；新增最新产品发布信息

### 冲突待确认
- [[PageC]] — 知识库记载 X，联网显示 Y，需人工核实

### 已验证准确
- [[PageD]] — 联网搜索确认现有内容仍然准确
- [[PageE]] — 未找到更新的相关信息，维持现状

### 建议后续动作
1. 核实 [[PageC]] 中的冲突项
2. 考虑为 [[PageF]] 补充更多来源（当前仅 1 个来源）
3. [[PageG]] 超过 180 天未更新且联网无新信息，建议标记为 `archived`
```

---

## 快变领域识别

以下标签/关键词标识的页面应被识别为"快变领域"，缩短刷新周期：

| 领域 | 标签关键词 | 建议刷新周期 |
|------|-----------|-------------|
| AI/机器学习 | ai, ml, llm, 机器学习, 深度学习 | 30 天 |
| 科技产品 | tech, 产品, 工具, 框架 | 45 天 |
| 商业/公司 | business, 公司, 市场, 融资 | 45 天 |
| 政策/法规 | policy, 法规, 政策 | 60 天 |
| 科学研究 | science, 论文, 研究 | 90 天 |
| 其他 | — | 180 天 |

---

## 强制约束

- **raw/ 不可变**：联网搜索获取的新信息写入 wiki/，绝不修改 raw/ 中的原始文件
- **冲突不静默覆盖**：发现矛盾时必须在 `## 知识冲突` 区块记录，等待用户确认
- **来源必须标注**：所有来自联网的信息必须标注 URL 来源
- **先读后写**：必须先完整读取现有页面内容，再决定如何更新
- **记录日志**：每次刷新操作必须在 `wiki/log.md` 追加记录
- **批量报告**：全部处理完毕后统一输出报告，不逐条打扰用户
